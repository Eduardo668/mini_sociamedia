import {BrowserRouter, Routes, Route} from 'react-router-dom';

const Router = ()=>{
    <BrowserRouter>
        <Routes>
            <Route path='/login' element />
        </Routes>
    </BrowserRouter>
}